# Spec: {{TITLE}}

| | |
|---|---|
| **ID** | {{ID}} |
| **Version** | 1 |
| **Status** | drafting |
| **Status changed** | {{STATUS_CHANGED}} |
| **Type(s)** | [GAP G-001: type not yet derived — feature / bug fix / refactor / other] |

<!--
This spec is filled by the Griller via draft-and-mark: every unknown is an inline
`[GAP G-###: what is missing]` marker. Ready requires zero open markers. Sections are
ordered by increasing detail (audience gradient) — a reader stops where their need ends.
-->

## Summary

<!-- One to three sentences, no technology. What is being done and why it matters. -->
[GAP G-002: summary]

## Description

<!-- The request in the requester's own terms: the ask, the why, the context. -->
[GAP G-003: description]

## Requirements & Acceptance Criteria

<!-- Stable IDs (R-###, C-###) that survive amendment. Every criterion names observable
     behavior a test can exercise and a delivered result can fail. -->
[GAP G-004: requirements and acceptance criteria]

<!-- Bug fixes (DoR 2.4) — give the fix requirement a failing scenario at the base revision:
     **Failing scenario (base revision):** input/state {…} → observed {…}, expected {…} -->


## Boundaries

### Excluded (with reasons)
<!-- Adjacent concerns deliberately out of scope: "out of scope, because X." -->
[GAP G-005: exclusions]

### Deferred (with reasons)
<!-- Negotiated out of *this* spec but kept alive, with reasons. -->

## Interface Surface

<!-- Does this work render or serve something a user observes? (DoR 8.1) One of three
     outcomes, and silence is not valid: **non-trivial** (renders or serves a user-observable
     interface → wireframe and visual evidence required) / **trivial, because ⟨reason⟩** (one
     obvious element with nothing to lay out) / **not applicable, because ⟨reason⟩** (nothing a
     user observes). Derived and human-verified like the type (G-001), re-derived after any
     change, not latched. -->

- **Interface-surface determination:** [GAP G-012: interface-surface determination — non-trivial / trivial, because ⟨reason⟩ / not applicable, because ⟨reason⟩; silence is not valid]

<!-- Non-trivial only (DoR 8.2): reference the static wireframe stored beside the spec (a
     sibling file, like the evidence directory) — grounds structure/fields/states/interaction,
     not colors/fonts/pixels.
     **Wireframe:** {path to the wireframe stored beside the spec} -->

## Classification

- **Declared type(s):** *see the Type(s) row in the header (G-001)*
- **Required test types:** [GAP G-006: derived from the type(s)]
- **For refactors — declared observable surface:** n/a unless refactor

## Evidence Plan

<!-- One promise per criterion: artifact (image/video/log/output) + what it will show +
     under what condition + the re-runnable command that generates it (Generated by,
     DoR 3.4) + the loop checkpoint it satisfies (Checkpoint, DoR 3.5). Falsifiable.
     For an interface-observable criterion when the Interface Surface is non-trivial, its
     promise carries a still image (and a video where feasible) referencing the approved
     wireframe — see the Interface Surface subsection (DoR §8.3). -->

| Criterion | Artifact | Will show | Under condition/action | Generated by | Checkpoint |
|---|---|---|---|---|---|
| [GAP G-007: evidence plan — one row per criterion] | | | | | |

## Harm Dispositions

| Class | Disposition | Requirements (if non-trivial) |
|---|---|---|
| Data integrity | [GAP G-008: disposition] | |
| Security | [GAP G-009: disposition] | |
| Performance | [GAP G-010: disposition] | |

## Adopter Bindings

- **Existing-behavior gate:** (from config)
- **Gate strength disposition:** (only if the gate is empty or trivially weak)

## Instantiation

<!-- Binds every ⟨term⟩ the definitions consume; carries the named rubrics. -->
[GAP G-011: instantiation — term bindings and the six rubrics]

## Amendments

| Version | Date | What changed, and the gap or rejection that drove it |
|---|---|---|
| 1 | (initial) | Initial draft |
